# Fake datasets

Transactions are generated on the fly by the `TxnGenerator`.  Command line flags enable scenarios:

* `--card-testing` – many small authorisations per card.
* `--impossible-travel` – alternating far apart geolocations for the same user.
